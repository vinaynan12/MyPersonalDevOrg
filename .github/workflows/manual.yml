# This is a basic workflow that is manually triggered

name: My Personal Org

on:
  push:
    branches: [ main ]
env:
  GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"

jobs:
  test_pull_request:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
      - name: Install SFDX CLI
      - run: npm install sfdx-cli --global
      - run: sfdx force:auth:jwt:grant --clientid 3MVG9ojmGst1I5HO05Iq5HjeRyUEXzBJOUHtAhXyHRpIUK.54KZ1kJ6PekO31DjiwfDRsGtUM55gbCBljdIAG --jwtkeyfile server.key --username vnarayanaswamy@suyati.com.devops1 --instanceurl https://test.salesforce.com -a Dev1
